{% include "planner/task_context.j2" %}

## Feedback on Previous Plan
{{ feedback }}

## Instruction
The feedback above identifies specific flaws in the previous plan. Each point represents a hard failure that MUST be fixed — do not carry forward any approach that caused a failure.

Produce a completely revised execution plan that addresses every feedback point. Re-read intent, entities, action_constraints, tool_matches, and risks from the task context before planning. For each feedback point, make a targeted change to the step(s) responsible.
