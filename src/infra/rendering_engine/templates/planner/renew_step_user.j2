## Task Context
Goal: {{ task.task_goal }}
Intent: {{ task.intent }}

## Current Step (order {{ step.order }})
{{ step_dict | tojson(indent=2) }}

## Feedback
{{ feedback }}

## Instruction
Revise the step above to address the feedback. Preserve the original objective unless the feedback explicitly requires changing it. Keep all fields — do not omit any key. Update `output_constraints` if the revised step produces a different artifact.
